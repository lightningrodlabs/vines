{
  "name"           : "we-applet",
  "version"        : "0.1.0",
  "private"        : true,
  "scripts"        : {
    "build"      : "vite build",
    "build:watch": "vite build --watch",
    "dist"       : "rm -rf dist && npm run build",
    "devtest"    : "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' concurrently \"npm run new-sandbox\" \"sleep 5 && npm run start:ui\"",
    "devtest:pp" : "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' APPLET_VIEW='ParticipationProtocol' concurrently \"npm run new-sandbox\" \"sleep 5 && npm run start:ui\"",
    "new-sandbox": "RUST_LOG=warn WASM_LOG=debug echo \"pass\" | ../$npm_package_config_bin/hc s --piped -f=$HC_ADMIN_PORT generate ../artifacts/vines.happ --run=$HC_APP_PORT -a VinesApplet network mem",
    "start:ui"   : "vite --port $UI_PORT --clearScreen false"
  },
  "devDependencies": {
    "vite"                   : "^4.5.2",
    "vite-plugin-static-copy": "0.16.0",
    "vite-plugin-checker"    : "^0.5.3",
    "bestzip"                : "^2.2.0",
    "concurrently"           : "^5.3.0",
    "deepmerge"              : "^4.2.2",
    "new-port-cli"           : "^1.0.0",
    "rollup"                 : "^2.56.2",
    "tslib"                  : "^2.4.0",
    "typescript"             : "~5.2.0"
  },
  "dependencies"   : {
    "@mdi/js": "^7.2.0"
  }
}
