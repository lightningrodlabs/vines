## editors
/.idea
/.vscode


## system files
.DS_Store

target/
node_modules/
dist/
.cargo/
.hc*
.kitsune2_bootstrap_srv
dna/workdir/dna*/*.dna
dna/workdir/happ*/*.happ
webapp/webhapp.workdir/*.webhapp
*.tsbuildinfo
.running
dna/*.txt


release/
artifacts/*
submodules/**/*
bin/

*.map


# Specific files
# Cargo.lock and package-lock.json are tracked on purpose: git dependencies are
# pinned by rev and CI builds with --locked / npm ci, so an upstream push cannot
# change what this repo builds without a commit here.
hash_zome*
