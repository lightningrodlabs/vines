{
  "extends"        : "../tsconfig.json",
  "compilerOptions": {
    "outDir" : "dist",
    //"declarationDir": "dist",
    "rootDir": "./src"
  },

  "include"        : [
    "src/**/*.ts",
    "src/generated/*.js"
  ]
}
